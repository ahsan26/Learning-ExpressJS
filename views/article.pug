extends layout

block content
    h1= article.title
    h4= article.author
    p= article.body
    a.btn.btn-default(href="/articles/edit/"+article._id) Edit
    a.btn.btn-danger.delete_btn(href="#", data-id=article._id) Delete